package main

import (
	"fmt"
)

func main() {
	fmt.Printf("i am bklet")
	/*
	wg := sync.WaitGroup{}
	wg.Add(1)
	quit := make(chan time.Time)
	go register.Register(quit, &wg, 1000)
	wg.Wait()

	 */
}
